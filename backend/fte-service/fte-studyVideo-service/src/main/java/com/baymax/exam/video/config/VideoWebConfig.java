package com.baymax.exam.video.config;

import com.baymax.exam.web.config.BaseWebConfig;
import org.springframework.context.annotation.Configuration;

/**
 * Web配置类
 */
@Configuration
public class VideoWebConfig extends BaseWebConfig {
    // 继承BaseWebConfig，确保拦截器被正确注册
} 